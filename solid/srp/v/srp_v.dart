class User {
  final  int id;
  final String name;

  User({required this.id, required this.name});

  void save() {
    // Save user to database
  }

 void add() {
    // add user to database
  }

  void sendEmail() {
    // Send email to user
  }
}
